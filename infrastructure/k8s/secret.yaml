# TEMPLATE — Do NOT commit real secrets to version control.
# Create this secret from your CI/CD pipeline or sealed-secrets tooling.
#
# kubectl create secret generic facesearch-secrets -n facesearch \
#   --from-literal=POSTGRES_USER=<value> \
#   --from-literal=POSTGRES_PASSWORD=<value> \
#   --from-literal=JWT_SECRET=<value> \
#   --from-literal=MINIO_ACCESS_KEY=<value> \
#   --from-literal=MINIO_SECRET_KEY=<value> \
#   --from-literal=WORKER_DATABASE_URL=<value>
#
# Or use Sealed Secrets / External Secrets Operator in production.

apiVersion: v1
kind: Secret
metadata:
  name: facesearch-secrets
  namespace: facesearch
type: Opaque
stringData:
  POSTGRES_USER: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME"
  JWT_REFRESH_SECRET: "REPLACE_ME"
  MINIO_ACCESS_KEY: "REPLACE_ME"
  MINIO_SECRET_KEY: "REPLACE_ME"
  DATABASE_URL: "postgresql://REPLACE_ME:REPLACE_ME@postgres:5432/facesearch"
  WORKER_DATABASE_URL: "postgresql://REPLACE_ME:REPLACE_ME@postgres:5432/facesearch"
