apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: facesearch
  labels:
    app: web
spec:
  selector:
    app: web
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
  type: ClusterIP
